project_name: "alex-hustle-hub"

languages:
- typescript

encoding: "utf-8"

line_ending:

language_backend:

ignore_all_files_in_gitignore: true

ignored_paths:
- node_modules/**
- dist/**

read_only: false

excluded_tools: []

included_optional_tools: []

fixed_tools: []

base_modes:

default_modes:

initial_prompt: "This is Alex Hustle Hub — a mobile-first teen hustle portal built for Jerry's daughter Alex. Vite + React + TypeScript + Tailwind. Deployed on Vercel (rekaldsis-projects/alex-hustle-hub). Key files: src/data/ideas.ts (hustle ideas data), src/pages/ (page components), src/components/BottomNav.tsx. Accent color: #FF2D78. No backend — pure static site."

symbol_info_budget:

read_only_memory_patterns: []
